type PlatformProvider {
  name: String!
  type: String!
  provider: String!
}

type Settings {
  platform_providers: [PlatformProvider!]!
  base_url_front: String!
  environment: String!
}

type Query {
  settings: Settings!
}
