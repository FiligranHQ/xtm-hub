type RolePortal implements Node {
    id: ID!
    name: String!
}

type RolePortalID implements Node {
    id: ID!
}

type Query {
    rolePortal(id: ID!): RolePortal @auth
    rolesPortal: [RolePortal!]! @auth
}
