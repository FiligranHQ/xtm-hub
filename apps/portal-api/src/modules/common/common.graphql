scalar JSON
scalar Date
scalar Upload

enum FilterKey {
  label
  organization_id
  personal_space
  serviceDefinition_identifier
}

input Filter {
  key: FilterKey
  value: [String!]!
}

type Success {
  success: Boolean!
}
