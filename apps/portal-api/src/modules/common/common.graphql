scalar JSON
scalar Date
scalar Upload

enum FilterKey {
  label
  organization_id
  personal_space
  integration_type
  integration_subtype
  slug
  product_version
  manager_supported
  feed_url
}

input Filter {
  key: FilterKey
  value: [String!]!
}

type Success {
  success: Boolean!
}
