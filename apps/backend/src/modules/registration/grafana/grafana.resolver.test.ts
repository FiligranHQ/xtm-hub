import { describe, expect, it } from 'vitest';
import resolver from './grafana.resolver';

describe('grafana.resolver', () => {
  it('should be defined', () => {
    expect(resolver).toBeDefined();
  });
});
