import { describe, expect, it } from 'vitest';
import { GrafanaApp } from './grafana.app';

describe('grafana.app', () => {
  it('should be defined', () => {
    expect(GrafanaApp).toBeDefined();
  });
});
