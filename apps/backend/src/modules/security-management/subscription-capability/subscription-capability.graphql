input AddSubscriptionCapabilityInput {
  subscriptionsId: [SubscriptionId!]!
  capabilitiesId: [ServiceCapabilityId]!
}

type Mutation {
  addSubscriptionCapability(
    input: AddSubscriptionCapabilityInput
  ): [SubscriptionModel] @auth
}
