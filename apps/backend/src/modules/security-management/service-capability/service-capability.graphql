scalar ServiceCapabilityId

type GenericServiceCapability implements Node {
  id: ID!
  name: String
}

type Query {
  userServiceCapabilities(
    service_instance_id: ServiceInstanceId!
  ): [UserServiceCapability] @auth
}
