query RegisteredPlatforms($input: RegisteredPlatformsInput!) {
  registeredPlatforms(input: $input) {
    id
    platform_id
    identifier
    title
    contract
    subscription {
      start_date
      end_date
      service_instance_id
    }
  }
}
